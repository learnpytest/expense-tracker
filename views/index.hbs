<div class="container mt-3">
  <div class="row">
    <div class="col-12 col-lg-8 mx-auto">
      <form action="/records/category"
            method="GET">
        <div class="form-group">

        </div>
        <div class="form-group">
          <select name="category"
                  id="category"
                  class="form-select bg-secondary"
                  style="color:black;"
                  onchange="this.form.submit()">
            {{> category}}
          </select>
        </div>

      </form>
      <a href="/dashboard">
        <input class="btn btn-info"
               value="Total Amount:"><span class="sr-only">(current)</span></input>
      </a>
      <h1 class="font-weight-bolder text-center"
          style="color:darkorange">$ {{addAmount recordsArr}}</h1>
    </div>
  </div>
  <ul class="list-group">
    {{!-- 用迴圈產生支出清單、顯示錯誤訊息提式使用者、新增支出元件 --}}
    {{> records}}
  </ul>
</div>